[package]
name = "two_pyramid_shell"
version = "2.0.0"
description = "2-Pyramid Win11 Context Menu COM Server"
authors = ["2-Pyramid Studio"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
windows = { version = "0.61", features = [
    "Win32_System_Com",
    "Win32_UI_Shell",
    "Win32_Foundation",
    "Win32_System_Ole",
] }
windows-core = "0.61"
